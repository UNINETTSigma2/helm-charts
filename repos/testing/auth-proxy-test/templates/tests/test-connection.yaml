apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "auth-proxy-test.fullname" . }}-test-connection"
  labels:
    {{- include "auth-proxy-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "auth-proxy-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
